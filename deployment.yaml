apiVersion: apps/v1
kind: Deployment
metadata:
  name: ipv6test-app
  namespace: default
  labels:
    app: ipv6test-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ipv6test-app
  template:
    metadata:
      labels:
        app: ipv6test-app
    spec:
      containers:
      - name: ipv6test-app
        image: us-central1-docker.pkg.dev/mechapower/apps/ipv6test
        resources:
          limits:
            memory: 32Mi
          requests:
            cpu: 10m
            memory: 6Mi
        readinessProbe:
          httpGet:
            path: /robots.txt
            port: 80
          initialDelaySeconds: 3
          periodSeconds: 3
        livenessProbe:
          httpGet:
            path: /robots.txt
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: ipv6test-app
  annotations:
    cloud.google.com/neg: '{"exposed_ports": {"80": {"name": "ipv6test-app-4azzo6t"}}}'
spec:
  type: ClusterIP
  selector:
    app: ipv6test-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80